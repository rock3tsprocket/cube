# cube 2
Rewrite of cube i think

## Set-up
1. Modify the values in examplesettings.json to whatever you desire and rename it to settings.json
2. Run `pip install -r requirements.txt` to install the bot's dependencies.
3. Run the bot by running `python main.py`
